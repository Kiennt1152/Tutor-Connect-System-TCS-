# Claude Code (local tooling config)
.claude/

# Postman (API testing artifacts)
.postman/
postman/

# Environment variables — KHÔNG commit
.env
backend/.env
frontend/.env

# Cấu hình mail bí mật (App Password) — KHÔNG commit
secret-mail.properties
secret-ai.properties

# Ảnh người dùng tải lên lúc chạy (avatar, giấy tờ xác minh, CCCD…) — KHÔNG commit
uploads/
backend/uploads/*
!backend/uploads/.gitkeep

# Log file sinh ra khi chạy local — KHÔNG commit
backend/*.log
