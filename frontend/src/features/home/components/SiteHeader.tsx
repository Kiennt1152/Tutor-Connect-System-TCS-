// Header chuẩn đã chuyển về shared/components (đúng tầng, tránh shared -> features).
// Giữ file này để các trang home không phải đổi import.
export { SiteHeader } from '../../../shared/components/SiteHeader';
export type { SiteHeaderNav } from '../../../shared/components/SiteHeader';
