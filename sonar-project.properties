# SonarQube Project Configuration for Tutor Connect System (TCS)
sonar.host.url=http://localhost:9000
sonar.login=squ_a4c46f33bc4e07e8625c3f67671a845db304a2cf
sonar.projectKey=tcs-system
sonar.projectName=Tutor Connect System (TCS)
sonar.projectVersion=1.0

# Encoding
sonar.sourceEncoding=UTF-8

# Source and Test directories
sonar.sources=backend/src/main/java,frontend/src
sonar.tests=backend/src/test/java
sonar.test.inclusions=**/*Test.java,**/*.test.tsx,**/*.test.ts,**/*.spec.tsx,**/*.spec.ts

# Java bytecode path for semantic analysis
sonar.java.binaries=backend/target/classes

# Exclusions (build output, static assets, seeds, migrations, third-party libraries)
sonar.exclusions=**/node_modules/**,**/dist/**,backend/target/**,**/scratch/**,**/*.sql,**/db/migration/**,**/docs/**,**/*.json,**/*.svg,**/*.png,**/*.jpg,**/*.ico
